{
  "tool": "utility.stopwatch",
  "description": "Controls the dashboard stopwatch (start, pause, or reset).",
  "permission_level": 0,
  "estimated_cost": 0.0,
  "requires_network": false,
  "supports_rollback": false,
  "rollback_action": null,
  "params_schema": {
    "type": "object",
    "required": ["action"],
    "properties": {
      "action": {
        "type": "string",
        "enum": ["start", "pause", "reset"],
        "description": "The control action for the stopwatch."
      }
    }
  }
}
