{
  "tool": "calendar.remove_event",
  "description": "Deletes an event from your calendar entirely using its title or 8-character ID.",
  "permission_level": 1,
  "estimated_cost": 0.0,
  "requires_network": false,
  "supports_rollback": false,
  "rollback_action": null,
  "params_schema": {
    "type": "object",
    "required": ["text"],
    "properties": {
      "text": {
        "type": "string",
        "description": "The event ID or substring title to remove."
      }
    }
  }
}
